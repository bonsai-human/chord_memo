# Chord Memo 仕様書

グリッドベースのコード進行メモツール。本書は公開中のビルド済みバンドル（`index-D360BJ6i.js`）の静的解析と実機動作確認から復元した仕様である。

**表記について**: 断定してある箇所はバンドル内のコードで確認済み。推測は「※推測」と明記する。

---

## 1. 技術スタック

| 項目 | 内容 |
|---|---|
| ビルド | Vite（`assets/index-[hash].{js,css}`、`base: '/chord_memo/'`） |
| UI | React 19（`react.transitional.element`、`createRoot` + `StrictMode`） |
| 音声 | Tone.js（standardized-audio-context 15.1.22 同梱） |
| アイコン | lucide-react |
| ホイールUI | Swiper（**同期音源の再生開始位置**を選ぶドラムロール型ピッカーにのみ使用） |
| 動画同期 | YouTube IFrame Player API |
| スタイル | CSSinJS なし。**インライン `style` 属性**が主体、一部 `<style>` タグを JSX 内に直接埋め込み |
| 永続化 | localStorage（プロジェクト）＋ IndexedDB（オーディオファイル実体） |

外部アセット依存が1つある。音源サンプルは自前ホスティングではなく以下から取得する。

```
https://nbrosowsky.github.io/tonejs-instruments/samples/
```

（tonejs-instruments。CC-BY 3.0。OSS化する際はライセンス表記が必要）

---

## 2. データモデル

### Project

```ts
interface Project {
  id: string;              // UUID
  name: string;
  description: string;
  tempo: number;           // 既定 120
  key: string;             // 既定 "C"。マイナーは末尾 "m"（例 "Am"）
  timeSignature: [number, number];  // 既定 [4, 4]
  instrument: InstrumentId;         // 既定 "piano"
  voicingOptimize: boolean;         // 既定 true
  voicingMin: number;               // MIDI番号。既定 48 (C3)
  voicingMax: number;               // MIDI番号。既定 72 (C5)
  loopEnabled: boolean;             // 既定 false
  metronomeEnabled: boolean;        // 既定 false
  audioUrl?: string;                // blob URL。実体は IndexedDB
  audioOffset: number;              // 秒。既定 0
  audioVolume: number;              // 0-100。既定 80
  audioEnabled: boolean;            // 既定 false
  youtubeUrl?: string;
  useYoutubeAudio: boolean;
  swingResolution: "8n" | "16n";    // 既定 "8n"
  measures: Measure[];
  chords: Record<string, Chord>;    // chordId → Chord
  createdAt: number;
  updatedAt: number;
}
```

### Measure

```ts
interface Measure {
  id: string;
  slots: Slot[];
  timeSignature?: [number, number]; // 省略時は前の小節から継承
  tempo?: number;                   // 同上
  key?: string;                     // 同上
  swing?: number;                   // 0-1。省略時 0
  swingResolution?: "8n" | "16n";
  label?: string;                   // セクションラベル（例 "Aメロ"）
  referenceLabel?: string;          // 参照先ラベル名
  referenceLoopCount?: number;      // 参照の繰り返し回数。既定 1
  isReferenceExpanded?: boolean;    // UI上で展開表示中か
}

interface Slot {
  chordId: string | null;
  duration: number;   // 4分音符 = 1 を単位とする長さ
}
```

### Chord

```ts
interface Chord {
  id: string;
  root: string;       // "C" 〜 "B"（♯/♭付き）
  quality: "" | "m" | "sus2" | "sus4" | "aug" | "dim";
  fifth: "" | "b5";
  seventh: "" | "7" | "M7" | "dim7";
  tensions: string[]; // "9" "b9" "#9" "11" "#11" "13" "b13"
  onChord: string;    // オンコードのベース音。空なら root
  isDimMode: boolean; // dim系表記モード
  isNC: boolean;      // N.C.（無音）
  omits: string[];    // "3rd" | "5th"
}
```

### 設計上の要点

- コードは `chords` に**辞書として1箇所だけ持ち**、スロットは `chordId` で参照する。同じコードを複数スロットが共有しうる。
- 保存時に `cleanupOrphanedChords()` が走り、どのスロットからも参照されていないコードを削除する。
- `duration` の単位は「4分音符 = 1」。小節の総 duration は `timeSignature[0] * (4 / timeSignature[1])` で決まる（3/4 なら 3、6/8 なら 3）。

### マイグレーション

読み込み時（`getAllProjects`）に旧データを補正する。

- `chords` の各要素に `fifth` `seventh` `tensions` `onChord` `isDimMode` の既定値を埋める。旧 `tension`（単数・文字列）は `tensions: [値]` に変換。
- `slots[].duration` が未定義なら 1。
- `instrument === "xylophone"` → `"electric-piano"` に変換。
- `measures` が空、または `measures[0].slots` が配列でないプロジェクトは読み込み時に**破棄**される。

---

## 3. 音楽理論ロジック

### 定数テーブル

```js
// クオリティ → 半音インターバル
QUALITY = { "": [0,4,7], m: [0,3,7], sus2: [0,2,7],
            sus4: [0,5,7], aug: [0,4,8], dim: [0,3,6] }

// 7th
SEVENTH = { "": null, "7": 10, "M7": 11, "dim7": 9 }

// テンション
TENSION = { "9": 14, "b9": 13, "#9": 15, "11": 17, "#11": 18, "13": 21, "b13": 20 }

// ルート → ピッチクラス
ROOT_OFFSET = { C:0, "C#":1, Db:1, D:2, "D#":3, Eb:3, E:4, F:5,
                "F#":6, Gb:6, G:7, "G#":8, Ab:8, A:9, "A#":10, Bb:10, B:11 }
```

`ROOT_OFFSET` は Proxy でラップされており、キー名末尾の `m` を除去してから引く（`"Am"` → `A` → 9）。

平行調マップも持つ。`C↔Am, Db↔Bbm, D↔Bm, Eb↔Cm, E↔C#m, F↔Dm, Gb↔Ebm, G↔Em, Ab↔Fm, A↔F#m, Bb↔Gm, B↔G#m`

### 音名表記の使い分け

キーごとに12音の表記（♯系か♭系か）を定義したテーブルを持つ。例：

- `C`: C, C#, D, Eb, E, F, F#, G, Ab, A, Bb, B
- `Db`: C, Db, D, Eb, E, F, Gb, G, Ab, A, Bb, B
- `D`: C, C#, D, D#, E, F, F#, G, G#, A, A#, B

Proxy 経由でマイナーキー（`Am` など）を平行長調に変換して引く。

### 構成音の算出（getIntervals）

1. `isNC` なら空配列。
2. `quality === "m" && fifth === "b5" && tensions.includes("13")` の特例 → `[0,3,6,9]`（dim7 相当）。
3. `isDimMode` なら quality を `dim` に上書き。
4. `QUALITY[quality]` を基底とする。
5. `fifth === "b5"` なら3番目の音を 6 に置換。
6. `omits` に `"3rd"` があればインターバル 2〜5 を除去、`"5th"` があれば 6〜8 を除去。
7. `SEVENTH[seventh]` を追加。
8. 各テンションを追加。ただし **`13` は 7th 未設定のとき 9（＝6th）として扱う**。
9. 重複除去してソート。

### コード名の生成（getChordName）

分岐は以下の順。

| 条件 | 出力例 |
|---|---|
| `m` + `7` + `b5` | `Cm7(b5)` |
| `isDimMode` + `dim7` | `Cdim7` |
| `isDimMode` | `Cdim` |
| `sus4` + `7` | `C7sus4` |
| `sus2` + `7` | `C7sus2` |
| `dim` + `7` | `Cm7(b5)` |
| その他 | `root + quality + seventh + tensions + fifth` |

末尾に `(omit3)` `(omit5)` を付加し、`onChord` が root と異なれば `/G` を付ける。`isNC` は `N.C.`。

### 度数表記（getDegreeName）

キーからの距離で `["I","bII","II","bIII","III","IV","bV","V","bVI","VI","bVII","VII"]` を引く。それ以降の組み立ては getChordName と同一。オンコードも度数化する。

### ダイアトニックコード

音度は メジャー `[0,2,4,5,7,9,11]` / マイナー `[0,2,3,5,7,8,10]`。各度数のルート音名はキーの表記テーブルから引く。和音は以下の4テーブルで固定。

| | I | II | III | IV | V | VI | VII |
|---|---|---|---|---|---|---|---|
| メジャー3和音 | – | m | m | – | – | m | dim |
| メジャー4和音 | M7 | m7 | m7 | M7 | 7 | m7 | m7(b5) |
| マイナー3和音 | m | dim | – | m | m | – | – |
| マイナー4和音 | m7 | m7(b5) | M7 | m7 | m7 | M7 | 7 |

（マイナーは自然短音階基準。III/VI/VII は bIII/bVI/bVII を指す）

ダイアトニックボタンを押すと、root / quality / seventh / fifth を設定し、**tensions・omits・onChord はリセット**される。

---

## 4. 小節設定の解決と伝播

ここが本アプリで最も込み入っている部分。

### 有効設定の解決

小節 N の実効的な key / tempo / timeSignature は、**先頭から N まで走査して最後に明示指定された値**になる。加えて「その値を N 自身が指定したのか（`isOverride`）」を返す。UI のチェックマーク表示に使う。

### 小節設定の更新

小節設定を変更するとき、以下が連鎖する。

1. **冗長排除** — 指定値が直前小節の実効値と同じなら、その指定を削除する（`undefined` にする）。設定が無駄に増えない。
2. **拍子変更** — スロット数を `timeSignature[0]` 本に増減し、各スロットの `duration` を `4 / timeSignature[1]` に揃える。さらに**後続の小節にも伝播**する（明示的な拍子指定を持つ小節に到達するまで）。
3. **キー変更** — 変更前後のキーの差分（半音）を求め、**その小節から次にキー指定がある小節の手前までのコードを自動移調**する。移調時は新しいコードオブジェクトを生成し（新UUID）、`chords` に追加する。移調後の音名は新しいキーの表記テーブルに従う。
4. `referenceLabel` を持つ小節（参照小節）は設定変更を拒否し、「参照小節の設定は変更できません」を表示する。

### 正規化（変更のたびに実行）

- コード・ラベル・参照のいずれかを持つ**最後の小節の +3 小節**を常に確保する。足りなければ空小節を追加、余分なら削除。これによりグリッド末尾に常に空きがある。
- 存在しないラベルを指す `referenceLabel` をクリアする。
- `referenceLabel` を持たない小節の `isReferenceExpanded` を false にする。

---

## 5. 再生エンジン

### イベント生成

再生対象は「コード・ラベル・参照のいずれかを持つ最後の小節」まで。何もなければ先頭1小節のみ。

参照小節（`referenceLabel` あり）は、`label` が一致する全小節を `referenceLoopCount` 回ぶん**その場に展開**する。展開1周目の先頭小節にだけ、参照元小節の key / tempo / timeSignature / swing を上書き適用する。

展開後の各小節を走査し、key / tempo / timeSignature を継承しながら、以下のイベントを積む。

- `chord` — コードが置かれたスロット（および先頭小節の先頭スロット）
- `progress_only` — コードのないスロット。直前の chord イベントの duration を延長したうえで、再生位置表示用に積む
- `metronome` — `metronome` オプション有効時、4分音符ごと。小節頭が強拍

秒への換算は `duration * 60 / tempo`。

### スウィング

スロットの開始位置（4分音符単位の小数部）に応じて開始時刻を遅らせる。`swing` は 0〜1。

| 解像度 | 対象位置 | 遅延量 |
|---|---|---|
| `8n` | 小数部 0.5 | `60/tempo * (1/6) * swing` |
| `16n` | 小数部 0.25 または 0.75 | `60/tempo * (1/12) * swing` |

イベントの duration は「次の位置のスウィング遅延 − 自分の遅延」を織り込んで補正される（前の音が伸びて後ろが縮む）。

### ボイシング

```
ベース音 = ROOT_OFFSET[onChord || root] + 36
コード音 = ROOT_OFFSET[root] + interval + 48   （各 interval について）
```

`voicingOptimize` が有効なとき、コード音のみ以下で畳み込む（ベース音は対象外）。

1. `center = (voicingMin + voicingMax) / 2`
2. 各音を `voicingMin` 以上になるまで +12、`voicingMax` 以下になるまで −12
3. さらに ±12 した候補が `[min, max]` に収まり、かつ `center` により近ければそちらを採る

最後にベース音と重複除去してソートし、MIDI番号を音名へ変換して発音する。

> ヘルプには「できるだけ音が繋がるように演奏されます」と書かれているが、**実装は前のコードのボイシングを参照していない**。各コードを独立に音域中央へ引き寄せているだけである。ボイスリーディングは実装されていない。

### 発音

- グリッド再生: `triggerAttackRelease(notes, max(0.1, duration - 0.05), time)`
- 単発プレビュー（スロット選択時）: `triggerAttackRelease(notes, "2n")`
- N.C.: `releaseAll()`
- メトロノーム: 強拍 `C5` / 弱拍 `C4`、`"32n"`、velocity 1.0 / 0.7。三角波の PolySynth（release 0.1）

### ループ

`loopEnabled` 時は Tone.Transport の `loop` を使い、`loopStart = 0` / `loopEnd = totalDuration`。

---

## 6. 同期再生

### オーディオファイル同期

- ファイルは IndexedDB に projectId 紐付けで保存。プロジェクト選択時に取り出して blob URL 化し、`Tone.Player` に読ませる。
- **開始位置の自動検出**: 先頭から走査し、振幅が **−40dB** を超える最初のサンプル位置を秒で返す。これを `audioOffset` の初期値に使う。
- 再生時は Transport に `sync()` して `offset + 開始位置` から鳴らす。

### YouTube 同期

`https://www.youtube.com/iframe_api` を動的ロード。以下のループで補正する。

1. プレイヤーが `playing` になる、または **3秒経過**するまで Transport を待機（`pause`）させる。
2. 開始時、YouTube の現在時刻から `audioOffset` を引いた位置に Transport をシークして開始。
3. 以降 `requestAnimationFrame` ごとに `Math.abs(youtubeTime - (transportTime + offset))` を監視し、**0.05秒を超えたら Transport を再シーク**して追従。
4. 末尾（`totalDuration - 0.05`）に達したら、ループ有効なら `seekTo(offset)`、無効なら停止。

> ヘルプの FAQ にある「再生直後は挙動が不安定」はこの待機・同期処理に由来する。

---

## 7. UI 構成

### レイアウト

```
┌─ Sidebar（左、モバイルは自動で閉じる） ─┬─ Main ──────────────┐
│  MY PROJECTS                              │  Header             │
│  ├ プロジェクト一覧（選択/名前編集/削除） │  ChordGrid（可変高） │
│  ├ ＋ 新規作成                            │  ActionBar          │
│  ├ プロジェクトを書き出し                 │  ChordKeyboard      │
│  └ JSONを読み込み                         │                     │
└───────────────────────────┴─────────────────┘
```

### 配色（Tailwind slate 系）

| 用途 | 値 |
|---|---|
| 背景 | `#0f172a` |
| グリッド背景 | `#020617` |
| パネル・小節 | `#1e293b` |
| 罫線・ボタン | `#334155` |
| ボタン hover | `#475569` |
| アクセント（選択・アイコン） | `#38bdf8` |
| 再生ボタン | `#10b981` |
| 本文 | `#e2e8f0` / 補助 `#94a3b8` |
| トースト | 背景 `#334155`、画面下中央固定 |

### ヘッダ

`height: 60px` / `background: #0f172a` / `border-bottom: 1px solid #334155` / `padding: 0 20px` / `gap: 15px`。

左端にハンバーガー、`flex: 1` のスペーサーを挟んで右側に5つ並ぶ。

| # | ボタン | アイコン | スタイル |
|---|---|---|---|
| 1 | サイドバー開閉 | Menu (20) | `#94a3b8`、背景なし |
| 2 | ヘルプ | HelpCircle (20) | `#94a3b8`、背景なし |
| 3 | 同期音源設定 | Music (20) | **音源設定済みなら背景 `#1e293b`＋色 `#a78bfa`、未設定なら透明＋ `#475569`** |
| 4 | 全般設定 | Settings (20) | `#94a3b8` |
| 5 | 再生 / 停止 | Play / Pause (18) | `padding: 8px 20px`、`border-radius: 8px`。停止中 `#10b981` ＋ `0 4px 6px rgba(16,185,129,0.2)`、**再生中は `#ef4444` ＋ `0 0 15px rgba(239,68,68,0.4)` に変わり Pause アイコンになる**。音源ロード中は disabled |
| 6 | 範囲選択モード | SquareDashed (18) | ON なら背景 `#38bdf8` ＋ 文字 `#0f172a`、OFF なら `#334155` ＋ 白。`border-radius: 8px`。再生中は無効 |

> 以前この欄に「キー設定・テンポ設定・拍子設定に相当するボタン群」と書いていたが誤り。ヘッダにその3つはない。キー・テンポ・拍子はグリッド行ヘッダのクリック、またはアクションバーの「設定を追加」から開く。

### サイドバー

幅 260px。上部にタイトル `Chord Memo`（`#38bdf8`、太字）。

- `MY PROJECTS` 見出し（小さめ・`#94a3b8`）＋ 右端に新規作成の Plus ボタン
- プロジェクト一覧。選択中の項目は背景 `#1e293b`＋角丸。ホバー/選択時に右端へ Pencil（名前編集）と Trash2（削除）
- `プロジェクトを書き出し`（Download 14px）と `JSONを読み込み`（Upload 14px）
  - いずれも `background: #1e293b` / `border: 1px solid #334155` / `color: #38bdf8` / `font-size: 0.8rem` / `padding: 8px 12px` / `border-radius: 6px`
  - 読み込みは `<label>` ＋ 非表示の `<input type="file" accept=".json">`
- モバイル（768px 未満）でプロジェクトを選択すると自動で閉じる

### グリッド

グリッドは **chunk（行）** 単位で構築する。chunk は「実効設定（key / tempo / timeSignature）が変わらない小節の連なり」。設定が1つでも変わると新しい chunk になる。

- chunk は該当する小節を**すべて**含み、その内部を `grid-template-columns: repeat(2, 1fr)`（モバイル `1fr`）で折り返す。つまり **1行 = 2小節（PC）/ 1小節（モバイル、`window.innerWidth < 768`）**。小節数が奇数なら最後の行の右半分は空白になる
- chunk 先頭にヘッダ行（背景 `#334155`、角丸 `8px 8px 0 0`）。**前の chunk から変化した項目だけ**を表示する
  - `Key= C` … `#38bdf8` 太字
  - `♩=120` … `#fbbf24`
  - `4/4` … `#94a3b8`
- コンテナは `max-width: 1000px`、padding は PC 20px / モバイル 10px
- スロット DOM id は `slot-{measureId}-{index}`（参照展開中は `-{loopCurrent}` が付く）

#### スロット

- 幅は `flex: duration`。基準スロット幅 = `(1 / slots.length) × コンテナ幅`（PC 968px、モバイル `innerWidth - 32`）
- 高さは `rowHeight`（既定 40px）を最小値とし可変
- 背景: 再生中 `#4f46e5` / 範囲選択内 `#312e81` / 通常 透明
- 選択中は `outline: 2px solid #6366f1`
- 再生中は下端に 4px のバー `#818cf8` ＋ glow
- 2本目以降のスロットに左罫線 `1px solid #334155`

#### コード名の表示

コードのあるスロットは、**後続の空スロットぶんまで表示幅を確保**する（`totalDisplaySlots = 1 + 連続する空スロット数`）。フォントサイズは以下で決まる。

```
基準 = モバイル 0.95rem / PC 1.1rem
利用可能幅 = totalDisplaySlots × スロット幅 − 4
推定幅 = 文字数 × 基準 × 16 × 0.5
推定幅 > 利用可能幅 なら 基準 = 利用可能幅 ÷ (文字数 × 16 × 0.5)
最小 0.5rem
```

さらに「1スロット幅 −（モバイル 50 / PC 100）px」を超える場合は `needsExpansion` となり、`width: max-content` ＋ `max-width: totalDisplaySlots × 100%` ではみ出し表示する。

#### ラベルと参照の見た目

- ラベル付き小節は左端に 4px 幅の縦帯 `#38bdf8`
- ラベル名は小節の上辺にタブ状バッジで表示（`#38bdf8` 背景、連続する同名ラベルは**先頭の小節のみ**）
- 参照小節（未展開）は `opacity: 0.6` ＋ 半透明オーバーレイ `rgba(15,23,42,0.4)`。**先頭スロットの位置**に `※ ラベル名 × N` のバッジを出し、クリックで展開
- 参照小節の再生中は、そのバッジの下に `2/4`（現在の周回 / 総回数）を `#fbbf24` で表示
- 展開中の2周目以降の先頭には「展開中 (タップで閉じる)」バッジ
- **展開中はグリッドの下に `● 参照セクションを展開表示中` というステータス行が出る**（`#38bdf8`、左寄せ）

ラベルは2列レイアウトの各列に対して縦帯が引かれるため、ラベル範囲が両列にまたがると**縦帯が2本見える**。ラベル名のバッジは chunk ヘッダに重なる位置（左上）に出る。

### アクションバー（8ボタン）

| ボタン | 動作 | 長押し／右クリック |
|---|---|---|
| リズムを変更・分割 | 小節のリズム細分化 | — |
| コピー | 選択範囲をコピー | — |
| 貼り付け | そのまま貼り付け | 「キーに合わせて貼り付け」（移調貼り付け） |
| 元に戻す | undo | 履歴一覧を表示 |
| やり直し | redo | 履歴一覧を表示 |
| 削除 | コードのみ削除 | 「小節ごと削除」 |
| 設定を追加 | 小節設定メニュー | — |
| ラベル設定 | セクションラベル | 範囲選択時のみ有効 |

グリッドとコードキーボードの間に置かれた、**中央寄せの角丸パネル**（背景 `#1e293b`、幅 400px 前後）に8つのアイコンボタンが横一列に並ぶ。

- 実行可能なボタンは `#38bdf8`、**削除だけ `#ef4444`**
- 実行できないボタンはグレー（`#475569` 系）に落ちる。コピーバッファが空なら貼り付け、履歴がなければ元に戻す／やり直し、範囲選択中でなければラベル設定が無効になる

長押しメニューはボタンの上に開く小さなパネル（背景 `#1e293b`、角丸、幅200px前後）。

| メニュー | 項目 |
|---|---|
| 貼り付け | `そのまま貼り付け`（白） / `キーに合わせて貼り付け`（`#38bdf8`） |
| 削除 | `コードのみ削除`（白） / `小節ごと削除`（`#ef4444`） |

リズム分割の選択肢は5種。`duration` は以下になる。

メニューは各行が「ラベル ＋ 右端にグレーの補足」という形で、以下の順に並ぶ。

| 表示 | 補足 | 内部値 | duration |
|---|---|---|---|
| 4分音符 | 1拍 | `div4` | 1 |
| 8分音符 | 0.5拍 | `div8` | 0.5 |
| 4分3連 | 2拍を3分割 | `div4t` | 2/3 |
| 8分3連 | 1拍を3分割 | `div8t` | 1/3 |
| 16分音符 | 0.25拍 | `div16` | 0.25 |

分割数は `小節長 / duration` を四捨五入。最後のスロットは端数を吸収する（最小 0.1）。

#### 長押しの実装

専用フックがボタンに以下のハンドラ一式を付与する。

- **しきい値 600ms**。押下から600ms経過で長押しメニューを開く
- 押下位置から **10px 以上動いたらキャンセル**
- 長押し発火時に `document.body` へ `long-press-active` クラスを付ける
- タッチ操作の後 **500ms 間はマウスイベントを無視**する（ゴーストクリック対策）
- `onContextMenu`（右クリック）でも `preventDefault()` して同じメニューを開く
- スタイルは `touchAction: none` / `userSelect: none` / `WebkitTouchCallout: none` / `WebkitTapHighlightColor: transparent`

### コードキーボード

スロット未選択時は「拍を選択してください」（破線枠、高さ100px）を表示。

#### 上段: ルート / ベース選択

左がルート（`#38bdf8`、1.5rem 太字）、右がベース（設定時 `#f472b6`、未設定は `/ -` を `#64748b`）。両脇の `♭` `♯` で半音上下する。

- 上げるときは ♯系表記、下げるときは ♭系表記で音名を決める
- **ルートを変更すると onChord も同じ音に揃う**（オンコードが解除される）
- ベース側の変更は onChord のみ動かす
- 度数表記モードのときは度数で表示する

#### 下段: 3タブ（`diatonic` / `quality` / `tension`）

タブバーは**画面幅を3等分**する（`flex: 1`）。背景 `#0f172a`、選択中は背景 `#1e293b` ＋ 文字 `#38bdf8` ＋ 下線 `2px solid #38bdf8`、非選択は `#64748b`。

キーボード本体はグリッドと違い `max-width` を持たず**画面幅いっぱい**に広がる。ボタンはいずれも 4列グリッド（`gap: 8px`）で、背景 `#1e293b`、選択中は `#38bdf8` ＋ 文字 `#0f172a`。

**ダイアトニック** — 実効キーのダイアトニック7和音 ＋ `3和音`/`4和音` トグル（計8ボタン、4列×2行）。

**クオリティ** — 各ボタンは連打で状態が巡回する。

| ボタン | 遷移 | 副作用 |
|---|---|---|
| `M/m` | `""` ⇄ `m` | dimMode 解除。seventh が `dim7` なら解除 |
| `sus4/sus2` | `""` → `sus4` → `sus2` → `""` | sus2 にすると tensions から `9` を除去。sus 系にすると omits から `3rd` を除去 |
| `aug/b5` | なし → `aug` → `b5` → なし | omits から `5th` を除去。解除時は quality を `m` または `""` に戻す |
| `dim/dim7` | なし → `dim` → `dim7` → なし | tensions・omits・fifth を全クリア |
| `omit3/5` | なし → `omit 3` → `omit 5` → なし | omit3 のとき sus 系なら quality を `""` に。omit5 のとき aug なら quality と fifth を `""` に |
| `m7(b5)` | 一発設定 | quality=`m`, seventh=`7`, fifth=`b5`、omits クリア |
| `N.C.` | 一発設定 | 他をすべてクリア。ボタン背景 `#f472b6` |

ボタンのラベルは状態を反映して変わる（`sus4/sus2` → `sus4`、`omit3/5` → `omit 3` など）。

**テンション** — 同じく連打で巡回する。

| ボタン | 遷移 |
|---|---|
| `7th` | `""` → `7` → `M7` → `""` |
| `9th` | `""` → `9` → `b9` → `#9` → `""` |
| `11th` | `""` → `11` → `#11` → `""` |
| `13th` | `""` → `13` → `b13` → `""` |

> ヘルプにある「音楽理論上あり得ないコードは組み合わせられません」の実体は、**`isDimMode` のときテンションタブの全ボタンが `disabled`（`opacity: 0.3`）になる**ことである。これ以外に組み合わせを禁止する仕組みはない。各クオリティボタンの副作用（sus2 で 9th を落とす等）が実質的な整合性維持を担っている。

なお `9th` を押したとき quality が `sus2` なら quality が `""` に戻る（sus2 と 9th が同じ音になるため）。

### 小節設定メニュー（設定を追加）

まず**ドロップダウンメニュー**が開く（モーダルではない）。アクションバーの `設定を追加` からのほか、**グリッドの chunk ヘッダをクリック**しても開き、対象の chunk ヘッダ下辺に `#38bdf8` のバーが出て選択中であることを示す。

メニュー項目は `キー設定` / `テンポ設定` / `拍子設定` / `スウィング` / `参照再生` の5つ。背景 `#1e293b`、項目間に区切り線。

- **すでに設定されている項目はオレンジ系（`#fb923c`）の文字＋右端にチェックマーク**
- 未設定の項目は白文字

項目を選ぶと、その type の設定モーダルが開く。設定モーダルとセクションラベルは**単一の共通モーダル**が `type` で切り替えて描画する（`key` / `tempo` / `timeSignature` / `swing` / `reference` / `label`）。共通の作りは以下。

- ヘッダ（背景 `#334155`）にタイトルと閉じるボタン
- 本体に type 別の入力
- フッタに `キャンセル`（`#334155`）と `適用`（`#3b82f6`）
- その下に `設定を削除`（`#ef4444`）。ただし**先頭小節の key / tempo / timeSignature では非表示**（曲全体の初期値なので消せない）

| type | 入力 |
|---|---|
| `key` | `メジャー` / `マイナー` トグル ＋ ルート選択。切り替え時は同じピッチクラスの平行調へ読み替える |
| `tempo` | 数値のテキスト入力1つだけ（`120` など） |
| `timeSignature` | 分子はテキスト入力（`inputMode="numeric"`、1〜32、幅60px、中央揃え）、`/` を挟んで分母は `<select>` で `2 / 4 / 8 / 16` |
| `swing` | 解像度（`8分` / `16分`）＋ スウィング率スライダ（0〜1、step 0.01、既定 0.5、%表示） |
| `reference` | 参照先セクションを `<select>` で選択（**自分自身のラベルを除いた**既存ラベル一覧）＋ ループ回数（`inputMode="numeric"`、1〜99） |
| `label` | セクション名のテキスト入力（placeholder「セクション名 (例: Aメロ)」、`autoFocus`） |

### 全般設定モーダル

ヘッダは Settings アイコン ＋ `全般設定` ＋ 閉じる X。

上半分は **2列×2行のカード**。各カードはチェックボックス ＋ 太字ラベル ＋ その下に小さな説明文。

| ラベル | 説明文 |
|---|---|
| ループ | 最後まで自動ループ |
| メトロノーム | クリック音を鳴らす |
| ボイシング | 音域を自動調整 |
| 度数表記 | I, IV, V... で表示 |

下半分は一段暗いパネルにまとめられている。

| 項目 | UI |
|---|---|
| 音色 | 右寄せの `<select>`。Piano / Acoustic Guitar / Organ / Distortion Guitar / Violin / Synth Lead |
| 音量 | 右端に `80%`（`#38bdf8`）、下に**青いスライダ**1本 |
| 音域制限 | 右端に `C3 - C5`（`#fbbf24`）、下に**スライダ2本**（上が上限、下が下限） |

フッタ右下に `設定を完了`（背景 `#38bdf8`、濃色文字）。

音色の内部IDと表示名の対応:

| 内部ID | 表示名 | サンプル |
|---|---|---|
| `piano` | Piano | A1-A6, C1-C6 |
| `guitar-acoustic` | Guitar | F4, G2-G3, A2-A3, C3-C4, D2-D3, E2-E3 |
| `organ` | Organ | C1-C6, A1-A3 |
| `guitar-electric` | Distortion Guitar | D#3-D#5, E2, F#2-F#5, A2-A5, C3-C6, C#2 |
| `violin` | Violin (Strings) | A3-A6, C4-C7, E4-E6, G4-G6 |
| `synth-lead` | Synth Lead | サンプルなし（合成音） |

### 同期音源設定モーダル

ヘッダは Music アイコン（`#a78bfa`）＋ `同期音源設定` ＋ X。

上部に2つのタブボタン。`ファイル`（Upload アイコン）と `YouTube`（YouTube ロゴ、アクティブ時は赤系）。選択中のタブは背景が明るくなる。

- **ファイルタブ** — 全幅の `音源を変更` ボタン（Upload アイコン）。未設定時は `オーディオファイルを選択`
- **YouTube タブ** — URL の1行入力（placeholder `YouTube URL`）

その下は共通のパネル。

| 項目 | UI |
|---|---|
| 音源ボリューム | 右端に `80%`（`#38bdf8`）、下に**紫のスライダ**（`#a78bfa`） |
| 再生開始位置 (秒) | **整数部と小数部の2つのボックス**（`#fbbf24`、幅50px、`padStart(2,"0")` で `0 . 22` のように表示）。下に**黄色のスライダ**（`accentColor: #fbbf24`、`min: 0`、`max: Math.max(30, audioOffset + 10)`、`step: 0.01`） |

**数値ボックスをクリックすると Swiper のドラムロールピッカーが開く。** 固定配置のポップアップ（背景 `#1e293b` / `border: 1px solid #475569` / 角丸 12px / 高さ120px・幅80px）で、`direction: vertical`・`loop`・`slidesPerView: 3`・`centeredSlides`・`mousewheel`。0〜99 を `padStart(2,"0")` で表示し、中央の値は `#fbbf24` 1.2rem 太字、他は `#64748b`。背後に透明なオーバーレイがあり、クリックで閉じる。

> 前の版でこのピッカーを「テンポ設定のUI」と書いていたが誤り。テンポ設定は単なるテキスト入力で、ドラムロールが出るのはここだけ。

### YouTube プレイヤー

YouTube 同期を設定すると、**画面右下にプレイヤーが表示される**（上辺に `YouTube 同期中` のラベル、幅200px前後、枠付き）。ヘルプの FAQ にある通り、規約上これを隠したり極端に小さくしたりはできない。

**プレイヤーはドラッグで自由に移動できる。** 上辺のラベル部分がドラッグハンドルとして機能する。右下は初期位置にすぎない。

### ヘルプモーダル

ヘッダは HelpCircle ＋ `操作マニュアル` ＋ X。

タブは **pill 型**（角丸 20px）。選択中は背景 `#38bdf8` ＋ 文字 `#0f172a` ＋ 太字、非選択は透明 ＋ `#94a3b8`。4タブ（基本操作 / コードキーボード / 小節設定 / その他）、計14セクション。

本文はスクロール領域。各セクション見出しは `#e2e8f0` ＋ 左にアイコン（`#38bdf8`、18px）＋ 下線 `1px solid #334155`。本文中の記法は以下のように変換される。

| 記法 | 変換結果 |
|---|---|
| `**強調**` | `#f8fafc` の `<strong>` |
| `[icon:Play]` | lucide アイコン（16px、`#38bdf8`、`vertical-align: middle`） |
| `[key:Space]` | キーキャップ風の span（背景 `#334155` / 枠 `#475569` / 等幅 / 0.75rem） |
| `* ` 始まり | 箇条書き（`•` ＋ 本文） |
| 改行 | `<br>` |

フッタ右下に `閉じる`（`#38bdf8`）。

### 書き出しモーダル

ヘッダは Download ＋ `形式を選択して書き出し` ＋ X。本体は**3枚の縦積みカード**。各カードは左に角丸の四角いアイコンチップ、右に太字タイトルと説明文。

| タイトル | アイコン色 | 説明 |
|---|---|---|
| プロジェクトデータ (JSON) | 青 | データのバックアップ用。直接ダウンロードします。 |
| rechord.cc 形式 | 緑 | Webサービス「rechord.cc」互換のテキスト。 |
| ChordPro 形式 | 緑 | 一般的なコード譜フォーマット。 |

### モバイルレイアウト（768px 未満）

- ヘッダは `flex: 1` のスペーサーが潰れ、6つのボタンが左から詰めて並ぶ
- サイドバーは閉じた状態が既定。プロジェクトを選ぶと自動で閉じる
- グリッドは1列（1行 = 1小節）。padding は 10px、スロット幅の基準は `window.innerWidth - 32`
- アクションバーの8ボタンは横一列のまま、サイズを詰めて収める
- コードキーボードのタブは3等分のまま、フォントを小さくする
- ダイアトニック・クオリティ・テンションのボタンは**モバイルでも4列**を維持する
- コード名のフォント基準は 0.95rem（PC は 1.1rem）、はみ出し判定のマージンは 50px（PC は 100px）

### アイコン一覧（lucide-react）

| 用途 | アイコン |
|---|---|
| サイドバー開閉 | Menu |
| 新規プロジェクト | Plus |
| プロジェクト名編集 / 削除 | Pencil / Trash2 |
| 書き出し / 読み込み | Download / Upload |
| ヘルプ | HelpCircle |
| 同期音源設定 | Music |
| 全般設定 | Settings |
| 再生 / 停止 | Play / Pause |
| 範囲選択モード | SquareDashed |
| リズム分割 | Scissors |
| コピー / 貼り付け | Copy / Clipboard |
| 元に戻す / やり直し | Undo / Redo |
| 削除 | Trash2 |
| 小節設定 | CirclePlus |
| ラベル設定 | Flag |
| モーダルを閉じる | X |
| ヘルプ本文中 | MousePointer, Play, Settings, HelpCircle, Music, Image |

### キーボードショートカット

| キー | 動作 |
|---|---|
| `Space` | 再生 / 停止 |
| `Ctrl + C` | コピー（範囲選択時） |
| `Ctrl + V` | 貼り付け |
| `Delete` / `Backspace` | コード削除 |
| `Shift + クリック` | 範囲選択（範囲選択モード時） |

---

## 8. コピー & 貼り付け

### 範囲選択

範囲は `selectedSlot`（始点）と `selectionEnd`（終点）の**2点で表す単一の連続範囲**である。始点・終点は逆順でもよく、判定時に正規化される。範囲選択モードでは、1回目のタップで始点、2回目で終点を置く。

> ヘルプの「範囲選択時であれば複数の範囲をコピー可能です」は、**複数小節にまたがる範囲**という意味。飛び地を複数選ぶ機能は実装されていない。

### コピー

範囲内の各小節について、スロット列と metadata（key / tempo / timeSignature）＋実効キーを保持する。参照した Chord も一緒にコピーバッファへ複製する。

**範囲に参照小節が含まれる場合はコピーを拒否**し、「参照関係を含む範囲はコピーできません。コードのみを選択してください。」を表示する。

### 貼り付け

2モードある。

- **そのまま貼り付け** — コードをそのまま。metadata が貼り付け先の実効値と異なる場合、その小節に設定として書き込む
- **キーに合わせて貼り付け（移調）** — コピー元の実効キーと貼り付け先の実効キーの半音差を求め、root と onChord を移調する。音名は貼り付け先キーの表記テーブルに従う。metadata は書き込まない

貼り付けは duration を保持したままスロット列に差し込む。挿入位置より前の既存スロットは分割され、末尾で小節長を超えた分は切り詰められ、足りなければ空スロットで埋められる。貼り付け先が足りなければ小節を自動追加する。

---

## 9. 履歴（Undo / Redo）

- プロジェクト全体を `JSON.parse(JSON.stringify(...))` でディープコピーしてスタックに積む
- **上限50件**。超えたら古いものから捨てる
- 各エントリはラベルを持つ（`"コード更新"` `"小節更新"` `"リズム分割"` `"貼り付け"` `"移調貼り付け"` `"コード削除"` `"小節削除"` `"一括更新"` など）
- 新しい操作をすると redo スタックは破棄される
- 長押しで履歴一覧を開き、**任意の時点へジャンプ**できる（間の履歴は反対側のスタックへ移動する）

履歴一覧はモーダル。ヘッダは History アイコン ＋ `操作履歴` ＋ X。

- 先頭の行は `● 現在の状態` ＋ 右端に `ACTIVE` バッジ。枠線 `#38bdf8` でハイライトされる
- 以下、各履歴が1行ずつ（左に Undo アイコン、背景 `#1e293b`、角丸）。ラベルをそのまま表示する
- フッタに `※新しい操作をすると後の履歴は破棄されます`（小さめのグレー文字）

---

## 10. 入出力

### JSON

`Project` をそのまま `JSON.stringify(project, null, 2)` して `{name}.json` でダウンロード。

読み込み時は **新しい UUID を振り直し**、名前に ` (Imported)` を付けて別プロジェクトとして追加する（上書きしない）。

### rechord.cc 形式

参照小節を展開したうえで出力する。

```
# Title: {name}
# Key: {key}
# BPM: {tempo}
# Meter: {n}/{d}

C - - - | F - - - | G - - - | C - - -
```

- 改行コードは CRLF
- コードのないスロットは直前のコードを引き継ぐ（先頭は `_`）
- スロット列は**可能なら圧縮**される。全スロットが同一なら1個に、前半後半が同じなら2個に、4等分・8等分で同じなら4個・8個に畳む
- 畳んだ後、直前と同じコードは `=` に置換
- 小節は ` | ` 区切り、**4小節ごとに改行**

### ChordPro 形式

```
{title: ...}
{key: ...}
{tempo: ...}
{time: 4/4}

[C] [F] | [G] [C] |
```

- 参照小節を展開して出力
- 途中で key / tempo / timeSignature が変わると、その位置にディレクティブ行を挿入する（4小節の区切りでない場合は改行を入れてから）
- コードが直前と同じスロットは出力しない（変化点のみ `[C]`）
- N.C. は `[N.C.]`

---

## 11. ストレージ

### localStorage

キー `chord_memo_projects` に `Project[]` を JSON で保存。全変更が即座に自動保存される。

### IndexedDB

同期音源のオーディオファイル実体を保存する。localStorage の容量制限を避けるため。

| 項目 | 値 |
|---|---|
| DB 名 | `ChordMemoAudioDB` |
| バージョン | 1 |
| オブジェクトストア | `audio_files`（keyPath なし＝out-of-line key） |
| キー | `projectId` |
| 値 | File / Blob そのもの |

API は `initDB` / `saveAudio(projectId, blob)` / `getAudio(projectId)` / `deleteAudio(projectId)` の4つ。プロジェクト選択時に取り出して `URL.createObjectURL()` で blob URL 化し、`Tone.Player` に渡す。

---

## 12. 既知の不具合

復元にあたって修正すべき点。

1. **PWA が機能していない** — `index.html` が `registerSW.js` で Service Worker を登録する一方、アプリ本体の起動時コードが `getRegistrations()` で取得した SW を**すべて `unregister()`** している。毎回登録と解除が競合する。ヘルプの FAQ には「PWA に対応しているためホーム画面に追加できます」と書かれており、説明と実装が食い違っている。`manifest.webmanifest` と `sw.js` はサーバー上に存在する（ともに 200）。

2. **音色マイグレーションの行き先が存在しない** — 旧 `xylophone` を `electric-piano` に変換するが、音色定義に `electric-piano` はない（実在するのは piano / guitar-acoustic / organ / guitar-electric / violin / synth-lead）。該当データを持つユーザーは音が鳴らなくなる。

3. **ヘルプの記述と実装の乖離** — ボイシングの説明「できるだけ音が繋がるように演奏されます」に対し、実装はボイスリーディングをしていない（5章参照）。実装を説明に合わせるか、説明を実装に合わせるかの判断が要る。

---

## 13. 未確定事項

主要画面・全モーダル・全コンテキストメニューを実機の画面で確認済み。残るのは以下だけ。

- トーストの自動消去時間（クリックで消せることは確認済み）
- 各モーダルの正確な寸法・余白（配色と構造は確定済み）
- キー設定モーダルのルート選択部分の並び（メジャー／マイナー切替の挙動はコードで確認済み）
- 再生中のグリッド追従スクロールの有無

---

## 14. 再実装の進め方（提案）

1. **基盤** — Vite + React + TypeScript。型定義（2章）とストレージ層（11章）、正規化・設定解決（4章）。ここが土台になる
2. **表示** — グリッド、コード名・度数表記（3章）、配色（7章）
3. **編集** — コードキーボード、リズム分割、履歴（9章）
4. **再生** — Tone.js 音源ロード、イベント生成、スウィング、ボイシング、メトロノーム（5章）
5. **参照・ラベル** — 展開ロジック、コピー&ペーストの制約（4・5・8章）
6. **同期** — オーディオファイル、YouTube（6章）
7. **入出力** — JSON / rechord.cc / ChordPro（10章）
8. **仕上げ** — ヘルプ、PWA（12章の1を修正）、モバイル対応

1〜3 で「メモとして使える」状態、4 で「音が鳴る」状態、5〜7 で元アプリと同等になる。
